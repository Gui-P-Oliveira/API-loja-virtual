import { Router } from "express";

const externalAPIRouter = new Router();

externalAPIRouter.post("/register", );

externalAPIRouter.post("/login", );

externalAPIRouter.post("/logoff", );

export default externalAPIRouter;

